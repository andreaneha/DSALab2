#include<studio.h>
#include"HashTableFunctions.h"
#include"hashTableAPI"

int hashFunction(size_t tableSize, int key){
    int key;
}

void destroyData(void *data){
    free(data);
}

void *printData(void *toBePrinted){
    int *data;
    data = (int*)toBePrinted;
    printf("data : %d\n", *data);


}
